import 'package:flutter/material.dart';

class RealTimeDisplay extends StatelessWidget {
  const RealTimeDisplay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
